# Core standalone settings. Home Assistant is NOT required.
SCD_DATABASE_PATH=./data/school_cycle_days.sqlite3
SCD_HOST=0.0.0.0
SCD_PORT=8088

# Optional Home Assistant adapter. Leave blank for a pure standalone install.
SCD_HA_URL=
SCD_HA_TOKEN=
SCD_VERIFY_SSL=true
